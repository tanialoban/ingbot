version: '2'

services:
    btelegr:
        build: btelegr
        container_name: btelegr
        hostname: btelegr
        restart: always
        environment:
            - NAME_BOT=
            - TELEGRAM_TOKEN=
            - MONGO_URI=
            
# TODO I exposed all secrets as env variables, please, remove them and regenerate all tokens. Don't keep them in public repository!!!!!
